# STRUCTURE.md — Directory Layout & Organization

> Generated by codemap | Last updated: 2026-07-02
>
> **Updated**: models.ts split into model-catalog + model-discovery (barrel re-export);
> auth.ts deleted — resolveApiKey moved to config-store.ts (credential medium).

## Project Root

```
pi-agy-provider/
├── src/                    # Source code (10 modules)
├── tests/
│   ├── unit/               # Unit tests (9 files, one per module)
│   ├── type/               # Type contract tests
│   └── e2e/                # End-to-end smoke tests (bash)
├── .github/workflows/      # CI pipeline (GitHub Actions)
├── .planning/codebase/     # This documentation
├── package.json            # Package metadata, scripts, dependencies
├── tsconfig.json           # TypeScript config (strict, noEmit)
├── vitest.config.ts        # Vitest test runner config
├── .oxlintrc.json          # Oxlint config (typescript + unicorn + import + jest)
├── .oxfmtrc.json           # Oxfmt formatter config
├── prek.toml               # Pre-commit hooks (whitespace, lint, format)
├── renovate.json           # Dependency update bot config
├── .npmignore              # npm publish exclusion list
├── .gitignore              # Git exclusion list
├── AGENTS.md               # Agent guide (commands, architecture, gotchas)
├── CONTEXT.md              # Domain glossary (ubiquitous language)
├── CONTRIBUTING.md         # Contribution guide
├── CHANGELOG.md            # Release history
├── RELEASE_CHECKLIST.md    # Release process checklist
├── README.md               # Project readme
└── LICENSE                 # MIT license
```

## `src/` — Source Modules

| File                | Lines | Purpose                                             |
| ------------------- | ----- | --------------------------------------------------- |
| `index.ts`          | 58    | Extension entry point — provider registration       |
| `models.ts`         | 16    | Barrel re-export from model-catalog + model-discovery |
| `model-catalog.ts`  | 113   | Static model definitions, types, thinking levels, MODELS |
| `model-discovery.ts`| 128   | Dynamic model discovery — fetch, retry, parse, resolveModels |
| `config-store.ts`   | 160   | Credential medium — file walking, OAuth, API key resolution |
| `oauth.ts`          | 110   | `/login`, `refreshToken`, `getApiKey`               |
| `env.ts`            | 76    | Constants, env helpers, URL builder, sanitizer      |
| `errors.ts`         | 61    | Error classification (4 types)                      |
| `error-handler.ts`  | 44    | Error surface pipeline (filter → classify → deliver) |
| `utils.ts`          | 27    | Shared type guards                                  |

**Total: ~793 lines of source code.** All modules under 300 lines.

> **Removed**: `src/auth.ts` (48 lines) — `resolveApiKey` consolidated into `config-store.ts`.

## `tests/` — Test Structure

| Directory / File       | Lines | Covers                                      |
| ---------------------- | ----- | ------------------------------------------- |
| `unit/index.test.ts`   | 86    | Provider registration, event wiring         |
| `unit/models.test.ts`  | 157   | Static models, remote fetch, resolveModels  |
| `unit/auth.test.ts`    | 90    | API key resolution chain (imports from config-store) |
| `unit/config-store.test.ts` | 119   | Path resolution, file walking, token extraction |
| `unit/oauth.test.ts`   | 160   | Login flow, refresh, getApiKey              |
| `unit/env.test.ts`     | 96    | Constants, resolveApiBase, sanitizeApiKey   |
| `unit/errors.test.ts`  | 57    | Error classification                        |
| `unit/error-handler.test.ts` | 105  | Error pipeline (filtering, notify, console) |
| `unit/utils.test.ts`   | 82    | Type guards                                 |
| `type/contract.ts`     | 5     | TypeScript contract validation              |
| `e2e/smoke.sh`         | 136   | Real API smoke tests (gemini-3.5-flash, gemini-3.1-pro) |

**Total: ~1,093 lines of test code.** All 9 source modules have corresponding unit tests.

## Naming Conventions

| Convention                | Example                             |
| ------------------------- | ----------------------------------- |
| File names                | kebab-case (`config-store.ts`)       |
| Source files              | `.ts` extension                     |
| Test files                | `*.test.ts` extension               |
| Directory names           | lowercase (`tests/unit/`)           |
| Entry point               | `src/index.ts`                      |
| Exports                   | Named exports from each module      |
| Provider name             | `"agy"` (short, DNS-safe, kebab)     |

## Key Locations

| What                          | Where                                          |
| ----------------------------- | ---------------------------------------------- |
| Extension entry point         | `src/index.ts` (default export)                |
| Provider registration         | `src/index.ts`                                 |
| Static model catalog          | `src/model-catalog.ts` (`MODELS` array)        |
| Dynamic model discovery       | `src/model-discovery.ts` (`fetchRemoteModels`, `resolveModels`) |
| API key resolution            | `src/config-store.ts` (`resolveApiKey`)        |
| Credential file paths         | `src/config-store.ts` (`defaultAuthPaths`)     |
| OAuth login flow              | `src/oauth.ts` (`login`)                       |
| Error classification          | `src/errors.ts` (`classifyGeminiError`)        |
| Error surface handler         | `src/error-handler.ts` (`handleGeminiError`)   |
| Environment constants         | `src/env.ts`                                   |
| CI workflow                   | `.github/workflows/ci.yml`                     |
| Pre-commit hooks              | `prek.toml`                                    |

## Configuration

| Purpose             | File                 | Key settings                                     |
| ------------------- | -------------------- | ------------------------------------------------ |
| TypeScript          | `tsconfig.json`      | `strict: true`, `noEmit: true`, `target: ES2022` |
| Testing             | `vitest.config.ts`   | Include: `tests/**/*.test.ts`                    |
| Linting             | `.oxlintrc.json`     | Plugins: typescript, unicorn, oxc, import, jest  |
| Formatting          | `.oxfmtrc.json`      | No custom ignore patterns                        |
| Pre-commit          | `prek.toml`          | trailing-whitespace, EOF, large files, JSON/TOML/YAML, oxlint, oxfmt |
| Dependencies update | `renovate.json`      | Extends `config:recommended`                     |
| npm publish         | `.npmignore`         | Excludes: `.git/`, `.github/`, `.planning/`, map files |
